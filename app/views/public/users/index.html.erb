<% @users. each do |user| %>
<%= attachment_image_tag user, :profile_image, size: "50x50", fallback: "default_profile.jpg" %>
氏名
<%= link_to user.last_name + user.first_name, user_path(user.id) %><br><br>
<% end %>