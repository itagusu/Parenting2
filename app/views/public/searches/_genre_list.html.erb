<div>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, genre_search_path(genre), class: 'text-decoration-nine text-warning' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>